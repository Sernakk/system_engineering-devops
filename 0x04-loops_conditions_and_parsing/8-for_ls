#!/usr/bin/env bash
# This script lists non-hidden files in the current directory, showing only the part after the first dash

for file in *; do
  # Skip if it's a directory or does not contain a dash
  if [[ "$file" == *-* ]]; then
    echo "${file#*-}"
  fi
done

